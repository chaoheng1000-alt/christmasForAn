import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// 如果部署在 GitHub Pages 子路径下，取消注释并设置你的仓库名
	// base: '/your-repo-name',
	// site: 'https://chaoheng1000-alt.github.io',
	
	// 输出模式：静态站点
	output: 'static',
});

